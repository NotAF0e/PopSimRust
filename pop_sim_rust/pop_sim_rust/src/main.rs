// use std::time::{Duration, Instant};

fn update_sim(){
    let _months_passed = 0;
    pub const POPULATION: i64 = -1;
    pub struct Sim {
    people: Vec<i32>,
    dead_people: Vec<i32>
    }
}

fn main() {

    // Graphing variables
    let mp: Vec<i32> = Vec::new();
    let pop: Vec<i32> = Vec::new();
    let tp: i32 = -1;


}
